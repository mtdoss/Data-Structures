function MinMaxStack() {
  this.values = [];
}


MinMaxStack.prototype.push = function(val) {
  if (this.values.length === 0) {
    values.push( {
      value: val,
      max: val,
      min: val
    });
  }
};
  
